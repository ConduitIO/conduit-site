import React from 'react';
import Alert from '../Alert';

export default function BetaAlert({ title, list }) {
    
    return (
        <Alert title={"Hey, this thing is in beta [will fix messaging here]"} />
    )
}
