---
title: "Create SQL Server Destination"
sidebar_label: "Create Connector"
sidebar_position: 4
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

The SQL Server Destination Connector allows you to load data from a pipeline into a SQL Server Table.

Before creating a destination connector:

1. [Setup](/docs/destinations/sqlserver/setup/overview) - Setup your SQL Server instance and acquire the credentials needed to talk to Meroxa.
2. [Add Resource](/docs/destinations/sqlserver/add-resource) - Add a SQL Server resource to your Meroxa Resource.

## Input

The input of SQL Server is a table name. 

## Creating Connector

Then, to configure SQL Server as a destination:

<Tabs
  defaultValue="cli"
  values={[
    {label: 'CLI', value: 'cli'},
    {label: 'Dashboard', value: 'dashboard'},
  ]}>
<TabItem value="dashboard">

Using the [Dashboard](https://dashboard.meroxa.io), perform these steps to add a new destination to a pipeline:

1. Navigate to a [pipeline](https://dashboard.meroxa.io/pipelines).
2. Add a destination connector:

<div style={{ textAlign: "center", padding: "20px 0px" }}>
  <img alt="Add SQL Server Connector to Pipeline" style={{ maxWidth: "500px" }} src="/images/docs/pipelines/add-destination.png" />
</div>

3. Name your new connector and select your SQL Server resource:

<div style={{ textAlign: "center", padding: "20px 0px" }}>
  <img alt="Configure SQL Server Connector to Pipeline" style={{ maxWidth: "500px" }} src="/images/docs/pipelines/configure-new-destination.png" />
</div>


4. Select "Create" and Done 🚀.

</TabItem>
<TabItem value="cli">

Using the [CLI](/cli/installation-guide), you can run the following command to add a new destination to a pipeline:

```shell
meroxa connector create to-sqlserver-connector \
 --to my-sqlserver \
 --input $STREAM_NAME \
 --pipeline my-pipeline
```

The command above creates a new destination connector called `to-sqlserver-connector`, sets the destination to a resource named `my-sqlserver`, and configures the input with a [stream](/docs/pipelines/streams).

</TabItem>
</Tabs>

