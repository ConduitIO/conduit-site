---
title: "Add SQL Server Resource"
sidebar_label: "Add Resource"
sidebar_position: 3
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

This guide will walkthrough adding a SQL Server resource to Meroxa. Before adding, see [SQL Server Setup](/docs/destinations/sqlserver/setup/overview).

## Credentials

To add a SQL Server resource, you will need an **Access URL**:

The Access URL contains the credentials needed to access your instance:

```
sqlserver://$MSSQL_USER:$MSSQL_PASS@$MSSQL_URL:$MSSQL_PORT/$MSSQL_DB
```

In the example above, replace following variables with valid credentials from your SQL Server environment:

- `$MSSQL_USER` - SQL Server Username
- `$MSSQL_PASS` - SQL Server Password
- `$MSSQL_URL` - SQL Server URL
- `$MSSQL_DB` - SQL Server Database Name
- `$MSSQL_PORT` - SQL Server Port (e.g., 5432).


## Adding Resource
<Tabs
  defaultValue="cli"
  values={[
    {label: 'CLI', value: 'cli'},
    {label: 'Dashboard', value: 'dashboard'},
  ]}>
<TabItem value="dashboard">

1. Navigate to the [Add Resource](https://dashboard.meroxa.io/resources/new) page.

![Add SQL Server Resource to Meroxa Catalog](/images/docs/sources/sqlserver/add-sqlserver.png)

2. Insert access credentials.

3. Select "Add".

</TabItem>
<TabItem value="cli">

Using the [CLI](/cli/installation-guide), you can run the following command to add a new resource:

```shell
meroxa resource add my-sqlserver \
--type sqlserver \
--url sqlserver://$MSSQL_USER:$MSSQL_PASS@$MSSQL_URL:$MSSQL_PORT/$MSSQL_DB \
```

In the command above, replace the following variables with valid credentials from your SQL Server environment.

</TabItem>
</Tabs>
 

You can view your resource with [`meroxa resource ls`](/cli/cmd/meroxa-resources-list) or in the [Dashboard](https://dashboard.meroxa.io/resources).
